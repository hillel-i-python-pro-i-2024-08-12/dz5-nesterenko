#найпростіший консольний додаток. повернення до спроби найпростішої реалізації

## Build
bash

docker build -t console-app .
docker run --rm console-app

## Docker compose
terminal

docker compose up
